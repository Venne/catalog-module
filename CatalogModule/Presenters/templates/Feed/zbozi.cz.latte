{contentType application/xml}
<?xml version="1.0" encoding="UTF-8"?>
<SHOP>
	{foreach $productRepository->findAll() as $entity}
	<SHOPITEM>
		<PRODUCTNAME>{$entity->name}</PRODUCTNAME>
		<PRODUCT>{$entity->name}</PRODUCT>
		<DESCRIPTION>{$entity->description}</DESCRIPTION>
		<URL>{link this route=>$entity->route}</URL>
		{if $entity->image}<IMGURL>{$server}{$entity->image->getFileUrl()}</IMGURL>{/if}
		<VAT>{$page->vat}</VAT>
		<PRICE_VAT>{$entity->price}</PRICE_VAT>
	</SHOPITEM>
	{/foreach}
</SHOP>
